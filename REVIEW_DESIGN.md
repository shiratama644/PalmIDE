# 設計書レビュー報告書 — Unified Autonomous Agent IDE v4.0（Phase 0〜4 通算）

> レビュー日: 2026-08-18 / レビュアー: シニア仕様エンジニア（AI） / 対象: `docs/*.md` 14ファイル + `README.md` + `PHASE*` 4件
> 観点: 保守性・可読性・実装可能性・セキュリティ・横断整合性・将来拡張性

---

## 1. 総評 — 5段階評価

| 観点 | 評価 | コメント |
|---|---|---|
| **情報保全** | ★★★★★ | 648行・コード12個・表2個・数値（600k/PBKDF2/AES-GCM等）を一字一句維持。Phase 3で自動検証済み。理想的。 |
| **情報アーキテクチャ** | ★★★★★ | 1章=1論理ドメインの原則が徹底。13→14ファイルへの分割は読み手の認知負荷を大幅に下げる。 |
| **相互参照** | ★★★★☆ | 全章またぎ言及を相対リンク化、READMEにマップ集約。Phase 4で `14_shared-contracts` を追加し単一ソース化も達成。残りは G05 の図の更新のみ。 |
| **実装可能性** | ★★★★☆ | Phase 4で Blocker 6件を叩き台で解消し、実行可能な粒度まで詳細化。残り4件は `⚠️` で明示され、誤実装を防ぐガードが機能。 |
| **セキュリティ** | ★★★★★ | BYOK暗号化・null origin隔離・persistの3点が一貫。G02/G16で復号・PAT保管まで補完。 |
| **将来拡張性** | ★★★★☆ | `RATE_TABLE` の外部定数化、`preferences` バリデーション、DoDテンプレートなど拡張時の差分が小さい設計。 |

**総合: 4.6 / 5.0 — 実装着手可能な高品質な仕様書**。残る4件の人間判断（特に G05 WebContainers矛盾）を決めれば、AIコーディングエージェントにそのまま渡せる。

---

## 2. 優れている点（Keep）

### 2.1 分割の粒度が適切
- **13→14ファイル構成**: 当初の648行1ファイルを、平均50行・最大115行の14ファイルに分割。`03_security-byok.md`（44行）と `04_storage-persist.md`（18行）の分離、`06` と `07` の分離は、参照タイミングが異なる論理ドメインを分けた好例。ファイル単体で `grep` しやすく、PRの差分も見やすい。
- **命名 `NN_kebab-case-slug`**: 連番が読む順序を示し、kebab-caseがURLフレンドリー。`14_shared-contracts` を末尾に置いた判断は既存番号を崩さず正しい。

### 2.2 各ファイルが単独で読める
- 冒頭の **親リンク・関連ファイルリンク・元行範囲コメント・1行要約** が全ファイルで統一。例: `09_patch-engine.md` を開いただけで「Fuzzy 3段階→関連は 08 と 07 と 05」と分かる。
- 相互参照は全て `[04_storage-persist.md](./04_storage-persist.md)` の相対リンクで、GitHub上でもIDEでもジャンプ可能。

### 2.3 Phase 4 の「原文 vs 補足」の分離が徹底
- `> 🆕` と `> ⚠️` の引用ブロックで視覚的に分離。原文は一切上書きせず、追記は末尾（出典直前）に集約。`git blame` で「元仕様か Phase 4 か」が一目で分かる。
- 横断的な契約を `14_shared-contracts.md` に集約したのは **最も優れた判断**。G04（JSON-RPC）、G08（TDD型）、G14（genAI）、G15（コスト）を分散させず単一ソース化したことで、将来の矛盾を未然に防いでいる。

### 2.4 固有名詞の裏取りが誠実
- Interactions APIが **2026-06-22 GA済み** であることを Web検索で確認し、公式の `client.interactions.create` と仕様書の `generateContent` の乖離を正直に補足。`store:false` の選択まで人間に委ねたのは推測の断定化を避ける好例。
- WebContainersの `crossOriginIsolated` 制約も StackBlitz公式で裏取りし、**現仕様のままでは物理的に起動しない** と明記。多くの仕様書が「動くはず」で済ませるところを、Blokerとして昇格させたのは高く評価できる。

---

## 3. 指摘事項（要改善 / 要人間判断）

### 3.1 🔴 最優先で決めるべきこと（Blocker）

| # | 指摘 | 対象 | 推奨アクション |
|---|---|---|---|
| **R1** | **WebContainersと null origin の矛盾（G05）** | 05_sandbox.md | **2層化（A案）を推奨**。`02_architecture.md` の図に「Host null origin iframe」と「WebContainers crossOriginIsolated iframe」の2段構成を追記する必要がある。現状の図のまま実装すると `SharedArrayBuffer` で必ず失敗する。Phase 5で図を更新すること。 |
| **R2** | **JSON-RPCのメソッド名が叩き台のまま** | 14_shared-contracts.md §1.4 | `typeCheck` / `runTests` / `validateOutput` / `extractSkeleton` / `applyPatch` / `lint` は妥当だが、**命名をチームで確定**すること。特に `extractSkeleton` は `ASTContextRouter` との命名整合を取るか要議論。 |
| **R3** | **TDD型契約の `skeleton` の粒度** | 14 §2 | `ContractPhaseOutput.skeleton` が `Record<string,string>` だが、Tree-sitter由来の JSON なのか Babel由来の文字列なのかが未決。**G03のランタイム選択と連動**して決めること。 |

### 3.2 🟡 改善推奨（要確認）

| # | 指摘 | 対象 | コメント |
|---|---|---|---|
| R4 | `normalizeWhitespace` の叩き台が簡易すぎる | 09_patch-engine.md G01 | 現状は `idx + searchBlock.length` でオフセットを推定しているが、正規化前後の差でズレる。**Phase 5で `14` に厳密版（オフセットマッピング表を持つ版）を追加**することを推奨。本番前にテストケース（全角空白・CRLF・タブ混在）で検証が必要。 |
| R5 | `cost` の `cached_tokens` の扱い | 07/10/14 | `cached_tokens * 0.04` のような割引後単価を `RATE_TABLE` に持たせたが、**Googleの課金が「割引率」なのか「別単価」なのか**は公式で再確認が必要。現状の `flexFactor * cached` は推測。実装前に請求書で検証を。 |
| R6 | `preferences` のマイグレーション | 12_db-schema.md G17 | `maxReviseRetries`（G09）や `RATE_TABLE` の追加は **DB v4→v5 のマイグレーション** が必要だが、まだ書かれていない。Phase 5で `12` に v5 ブロックを追加すること。 |
| R7 | READMEの図が2箇所管理 | README.md / 02_architecture.md | 同一図が2箇所に複製されている。G05の2層化で図を更新する際は2箇所直す必要があり、**いずれ README は `02` へのリンクのみにする** か、ビルド時に自動同期する運用を検討。 |

### 3.3 🟢 軽微な指摘（Nice to have）

- **R8**: `01_overview.md` は変更なしで良いが、Phase 4で追加された `14` への言及がなく、初学者が「共通契約」を読み飛ばす可能性がある。`01` の要約に「横断契約は `14` を参照」の1行を足すと親切（必須ではない）。
- **R9**: `12_db-schema.md` の `checksum` ライフサイクルは `files` にしか触れていないが、`snapshots` の整合性チェックも同様に必要。追記するとより堅牢。
- **R10**: `13_roadmap.md` のDoDは叩き台として十分だが、各Milestoneに **完了期限の目安（例: M1=2週間）** がない。プロジェクト管理上は期限を別表で持つと良い。

---

## 4. ファイル別レビュー（14ファイル）

| ファイル | 良い点 | 気になる点 | 判定 |
|---|---|---|---|
| 01_overview | 8原則が箇条書きで読みやすい | 変更なしで安定 | ✅ そのまま |
| 02_architecture | 図が一字一句保持、レイヤ関係が明確 | G05の2層化で図の更新が必要 | △ 将来更新 |
| 03_security-byok | G02で復号とエラーUIまで補完、5分破棄との整合も取れている | 特になし | ✅ 優秀 |
| 04_storage-persist | G18で `persist()===false` のトースト案まで具体 | 特になし | ✅ 優秀 |
| 05_sandbox | G03/G05を `⚠️` で正直に未解決として残したのは誤実装防止に有効 | R1の決定待ち | ⚠️ 要決定 |
| 06_model-selection | GA裏取りと `GoogleGenAI` 初期化コードが具体的 | 特になし | ✅ 優秀 |
| 07_model-caching-tier | コスト式を数式化し `RATE_TABLE` を外部化したのは将来の価格改定に強い | R5の割引率要検証 | ✅ 良好 |
| 08_ui-ux-workflow | G09で `maxReviseRetries=3` と具体化、TDD型は `14` へ集約でスッキリ | 特になし | ✅ 良好 |
| 09_patch-engine | G01の叩き台は最小動作として十分。横断参照も `14` へ | R4の厳密化は将来 | ✅ 良好 |
| 10_logging-scribe | G10を `⚠️` で3案提示、コストは `14` と整合 | R1と同様、単一タブ制約を早めに決めると良い | ⚠️ 要決定 |
| 11_storage-export | G16でPAT保管の3案を提示 | 要決定 | ⚠️ 要決定 |
| 12_db-schema | G12/G17が具体的、G11はSkip理由も明記で誠実 | R6のv5マイグレーション追加を推奨 | ✅ 良好 |
| 13_roadmap | DoDテンプレートが具体的でテスト可能な粒度 | R10の期限は別管理で | ✅ 良好 |
| 14_shared-contracts | **横断契約の単一ソース化は本プロジェクト最大の改善**。JSON-RPC/TDD/GenAI/コストが1ファイルに集約 | R2/R3の命名確定が残る | ✅ 優秀（要命名確定） |

---

## 5. セキュリティレビュー

- **暗号化**: `AES-GCM-256` + `PBKDF2 600k` + `256-bit Salt` + `96-bit IV` は全ファイルで一貫。G02で復号時の `OperationError` ハンドリングまで追記され、総当たり対策として十分。
- **隔離**: `sandbox="allow-scripts"` + `allow-same-origin`なし + `event.origin==="null"` 検証は一貫。**ただし G05の矛盾が解消されるまでは WebContainers 経路だけが隔離の穴になる**。2層化で解消できるため、実装前に必ずA案を採用すること。
- **永続化**: `persist()` の拒否時フォールバック（G18）が追記され、ユーザへの警告導線が確保された。

---

## 6. 実装への readiness チェック

| Milestone | 依存ファイル | 準備状況 |
|---|---|---|
| M1 基盤 | 01,02,03,04,06,14 | **Ready**（G02/G06/G14で初期化コードまで揃った） |
| M2 ファイル&承認 | 04,09,08 | **Ready**（G01/G09でパッチとリトライ上限が揃った） |
| M3 Sandbox | 05,14 | **Blocked**（G05の2層化決定待ち） |
| M4 オーケストレーション | 06,07,08,14 | **Ready**（G07/G15でコスト式まで揃った） |
| M5 ログ&外部連携 | 10,11,12 | **要決定**（G10/G16の選択待ち） |

> **結論**: M1/M2/M4 は明日からAIエージェントに渡して実装可能。M3/M5 は上記4件の人間判断後に着手。

---

## 7. 推奨次アクション（優先順位順）

1. **G05を決定する（30分MTG）** — 05_sandbox.md の2層化（A案）を承認し、`02_architecture.md` の図を更新するPRを切る。
2. **G03/G10/G16を決定する（各10分）** — 05/10/11 の `⚠️` を `🆕` に更新。決定内容は `14` にも反映。
3. **`14` のメソッド名を確定する（命名会）** — `typeCheck` 等6メソッドの最終命名を決め、Zodスキーマに起こす。
4. **v5マイグレーション雛形を追加** — `12_db-schema.md` に `maxReviseRetries` と `RATE_TABLE` 用の `preferences` 追加を v5 として追記。
5. **M1の雛形生成** — `03` の `decryptApiKey` と `12` の `openDatabase` を統合した `src/lib/security.ts` / `src/lib/db.ts` を生成。

---

## 8. 総評コメント

> 分割（Phase 0〜3）では「一字一句維持」という最も難しい制約を完璧に守りつつ、14ファイルへの再編で可読性を飛躍的に向上させた。さらに Phase 4 で「動かない仕様」を「動く仕様」に昇華させ、**AIが誤読する余白を徹底的に塞いだ** 点が秀逸。特に `14_shared-contracts.md` の新設は、複数AIエージェントが並列で実装しても矛盾しないための要石となる。
>
> 残る4件の `⚠️` は、むしろ「決めずに残した」ことが誠実さの証左。推測で埋めず選択肢を提示したため、実装時の手戻りが最小で済む。
>
> **この設計書は、明日から Claude Code / Cursor に渡して実装を開始できる品質に達している。** G05の図の更新だけを最優先で決めれば、後は Milestone 1 から順に着手できる。

---

> **レビュー更新履歴**: 本レビューは `PHASE4_report.md` までの成果を対象。G05等の決定後は本ファイルを改訂し、Ready の Milestone を更新すること。
